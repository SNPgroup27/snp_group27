# Conda environment for the lab (use from repo root: conda env create -f environment.yml)
name: snp_lab
channels:
  - conda-forge
dependencies:
  - python>=3.10,<3.13
  - pip
  - pip:
      - -r file:requirements.txt
